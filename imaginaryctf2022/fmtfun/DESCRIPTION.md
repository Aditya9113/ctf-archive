One format string to rule them all, one format string to find them. One format string to bring them all, and in the darkness bind them.

**Editor's Note:** The fmt_fun binary is the original version and the wrapper binary is the patched version. run the wrapper binary as the process to solve the challenge and if you require symbols, refer to the original fmt_fun binary.

---
**Author:** Eth007
